delete from resumo_calculo where id > 0;
delete from dados_processo where id > 0;
select * from dados_processo;
select * from resumo_calculo;

select r.descricao, d.reclamado, d.reclamante, d.processo
from dados_processo d inner join resumo_calculo r on d.id = r.dadosProcessoId
order by d.reclamado desc limit 24;